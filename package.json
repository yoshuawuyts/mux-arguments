{
  "name": "mux-arguments",
  "version": "1.0.0",
  "description": "Function argument multiplexer",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "yoshuawuyts/mux-arguments",
  "keywords": [
    "multiplex",
    "function",
    "arguments"
  ],
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "dependency-check": "^2.5.0",
    "istanbul": "^0.3.17",
    "standard": "^5.0.2",
    "tape": "^4.0.3"
  },
  "files": [
    "LICENSE",
    "index.js",
    "README.md"
  ]
}
